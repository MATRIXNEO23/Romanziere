# Initial bootstrap checkpoint

Romanziere continuity is being created before literary training.

Configured so far:
- neutral profile policy;
- restart/recovery entrypoint;
- frequent live-context engine;
- read-only source registry for GPTina, Tessa and the existing novel.

Pending: retrieval index, source import, regression tests and CI verification.

Do not begin literary training until bootstrap verification is complete.
